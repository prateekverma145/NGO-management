[build]
  command = "npm run build"
  publish = "dist"  # Vite builds to 'dist' by default, not 'build'

[build.environment]
  NODE_VERSION = "18"
  VITE_API_BASE_URL = "https://ngo-management-rg38.onrender.com"

[dev]
  command = "npm run dev"

